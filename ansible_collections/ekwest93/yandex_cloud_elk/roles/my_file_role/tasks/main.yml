---
- name: Create file using custom module
  ekwest93.yandex_cloud_elk.my_own_module:
    path: "{{ my_module_path }}"
    content: "{{ my_module_content }}"
  register: result

- name: Display result
  debug:
    var: result
